import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gps-cam-files-ionic6',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
